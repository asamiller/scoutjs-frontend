import React, { Component, PropTypes } from 'react';

require('../css/logo-bar.scss');

export default class LogoBar extends Component {
  render () {
    return (
      <div className='logo-bar'>
        NPF
      </div>
    );
  }
}

LogoBar.propTypes = {
  
};