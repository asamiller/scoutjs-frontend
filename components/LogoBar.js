import React, { Component, PropTypes } from 'react';

export default class LogoBar extends Component {
  render () {
    return (
      <div>
        NPF
      </div>
    );
  }
}

LogoBar.propTypes = {
  
};